# swallow 环境变量配置（给调度器用）
#
# 为什么需要它：cron / systemd / hermes cron 这类非交互调度器跑的是干净 env，
# 不会 source ~/.bashrc——你写在 ~/.bashrc 里的 ANTHROPIC_API_KEY 它们拿不到。
# 放这份文件后，orchestrator 启动会自动读进 process.env（已 export 的不覆盖）。
# 路径默认 ~/.config/swallow/swallow.env；想换路径：export SWALLOW_ENV_FILE=/path/to/your.env
#
# 拷到 ~/.config/swallow/swallow.env 后改成你的值：
#   cp ~/.local/share/swallow/swallow.env.example ~/.config/swallow/swallow.env && chmod 600 ~/.config/swallow/swallow.env

# ---- 鉴权 / 模型（必填）----
ANTHROPIC_API_KEY=sk-...                      # 换成你的密钥
# 走代理（GLM/自托管/中转）才填；官方 Anthropic API 不用：
# ANTHROPIC_BASE_URL=http://192.168.241.10:3000
# 指定模型（走代理时按代理支持的模型名填）：
# ANTHROPIC_MODEL=glm-5.1
# ANTHROPIC_DEFAULT_OPUS_MODEL=glm-5.1
# ANTHROPIC_DEFAULT_SONNET_MODEL=glm-5.1
# ANTHROPIC_DEFAULT_HAIKU_MODEL=glm-5.1
# CLAUDE_CODE_SUBAGENT_MODEL=glm-5.1
#
# 限额不在这里——写死在 orchestrator.ts 顶部常量（token 不限量场景下预算/轮数护栏纯属挡路，
# 一律 0=不限；行为护栏 STALL_LIMIT/ABORT_TIMEOUT_MIN/SESSION_RETRY_LIMIT 防卡死，
# FAILED_TASK_LIMIT/DLQ_SPLIT_LIMIT 防死信队列死循环）。
# 要改限额改代码，不读这个文件。
